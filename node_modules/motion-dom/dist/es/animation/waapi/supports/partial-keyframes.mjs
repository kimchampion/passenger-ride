function supportsPartialKeyframes() {
  if (typeof document === "undefined" || !document.createElement) {
    return false;
  }

  try {
    document.createElement("div").animate({ opacity: [1] });
    return true;
  } catch {
    return false;
  }
}

export { supportsPartialKeyframes };
